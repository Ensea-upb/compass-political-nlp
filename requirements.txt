# COMPASS Expert-Pays — dépendances (tout est état de l'art, rien from scratch)
# Python >= 3.10

# Colonne vertébrale
pydantic>=2.5
pydantic-settings>=2.1
PyYAML>=6.0

# C01 — pipeline documentaire
PyMuPDF>=1.24          # extraction PDF (fitz)
pytesseract>=0.3.10    # OCR par défaut (binaire tesseract + langues fra/eng)
# surya-ocr>=0.9       # optionnel : scans dégradés, 90+ langues (> Tesseract, benchmarks 2026)
Pillow>=10.0
trafilatura>=1.8       # extraction web
htmldate>=1.9          # datation des pages web (JOSS) — contrôle temporel C08/C15
lingua-language-detector>=2.0
spacy>=3.7

# C02/C03 — mémoires
chromadb>=0.5
sentence-transformers>=3.0
pandas>=2.1

# C06 — retrieval
rank-bm25>=0.2.2

# C07/C14 — suffisance et validation
scikit-learn>=1.4
scipy>=1.11
joblib>=1.3
numpy>=1.26

# C08 — recherche active
ddgs>=9.0              # DuckDuckGo Search (sans clé) ; Tavily optionnel via API

# C09/C10/C13 — NLI et classification
transformers>=4.44
torch>=2.2

# C10/C11/C15 — LLM multi-fournisseurs et traçabilité
litellm>=1.40          # clés API via variables d'environnement uniquement
structlog>=24.1

# C12 — agrégation
krippendorff>=0.6

# Tests
pytest>=8.0
